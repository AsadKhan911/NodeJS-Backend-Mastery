In this lecture we make an API generated by chatGPT.

We copy our routing code and add one more else if in this code for reading the api data.

We add this extra code in routing code:

const fs = require('fs') //import

let JSONdata = fs.readFileSync('API.json' , 'utf-8') //second we use readFile method to store its values in jsonData variable
let objectData = JSON.parse(JSONdata) //Third we parse means we convert into object

else if(req.url == '/apiinfo'){
        res.writeHead(200 , {"CONTENT-TYPE" : "application/json"})
        res.end(JSONdata) 
	or
	res.end(objectData[0].city) //if we want to show specific value we use object.
    } //then we use jsonData to display

POINTS TO REMEMBER:

1). If we want to show specific value we have to parse(convert) json to object and use object show speicific values of each object like this Object[0].key because when json is parsed it becomes array of objects.

2). If we want to show the whole API and want to display it we have to use the json file directly not the object file and if not availble we will convert object file to .stringfy() means will convert it in json.