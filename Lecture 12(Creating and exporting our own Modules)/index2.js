const func = require('./exportFile2') //When we store it in a variable func it will act as an object

console.log(func.add1(1,2)) //And we will simply call like this
console.log(func.sub(5,4))
console.log(func.name)