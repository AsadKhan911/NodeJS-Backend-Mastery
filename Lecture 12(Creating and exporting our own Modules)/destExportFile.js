const { add , sub , name } = require('./destructuring') //like this destructuring we can import in a single line

console.log(add(1,2)) //also with this we can use with dot porperty we use in objects commonly
console.log(sub(8,4))
console.log(name)