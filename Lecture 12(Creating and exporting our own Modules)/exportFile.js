//Making function here to export in index.js
const add = ( a , b ) => {
    return a + b
}
let name = 'Asad'
module.exports = add; //for exporting simply write module.exports = add(the exact name of func or variable)

//This file shows only if we want to export and use only 1 function or variable at a time