//Add function
const add = ( a , b ) => {
    return a + b
}

//Subtract function
const sub = ( a , b ) => {
    return a - b
}

//Variable
let name = 'Asad'

module.exports = { add , sub , name } //In this way we can simply export the fucn|var without doing seperatly for each by destructuring
